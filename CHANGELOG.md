# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。
<br><br><br><br><br>


## [1.2.1] - 2026-08-15

### logo图标更新：

**处理过程**：
- 更换了在ttkbootstrap中默认的树叶主题logo图标
- 图标文件icon.ico放在程序同目录下
- 代码里直接在__main__部分添加root.iconbitmap()调用

**设计背景**：
- 交谊舞（Ballroom Dance）的核心在于双人配合、优雅的框架、流畅的旋转。结合 pulse-dark 主题（通常指深色背景、高饱和霓虹/脉冲光效、科技感），固设计出既复古优雅又现代动感的图标。


**设计要点分析**：
- 色彩：采用深色底（#121212 或 #1A1A2E）+ 霓虹描边（紫红 #FF2A6D 或 青色 #05D9E8），这是 Pulse 主题的经典特征。

- 元素：抛弃复杂的细节，提取交谊舞的剪影轮廓——男士的燕尾服线条、女士的裙摆弧线、以及两人之间的“框架”（Frame）。

- 构图：在有限像素内，强调“圆”与“弧”，体现华尔兹或探戈的旋转感。


**确定方案**：
- 脉冲旋转（抽象几何，适合作为“舞种”分类）

**设计理念**：
- 因为需要表示“舞种排序”中的“旋转”或“切换”概念，将交谊舞的旋转轨迹抽象化为两个相交的环。男士的领结与女士的裙摆融合成几何图形。

**Pulse-Dark 主题下 GUI 图标的尺寸适配**：

- 大尺寸（32px+）：细节丰富，霓虹光效明显。

- 小尺寸（16px）：掉底部圆点，加粗描边至 6px-8px，保证清晰度。

**色彩规范**：

- 背景：
#12121A 或 #0D0D14（保持绝对深色，不刺眼）。

男士/冷静色：#05D9E8（脉冲青，代表引导、框架）。

女士/热情色：#FF2A6D（脉冲粉红，代表旋转、裙摆）。

- 点缀：#F9F871（用来做高光或排序指示点）。


**透明背景优化**：

- 移除背景：完全透明，只保留核心图形。

- 增强对比度：加粗了线条，确保在透明背景下不显得单薄。

- 优化细节：调整了领结和裙摆的比例，使其在 32px-64px 尺寸下依然可辨。
<br><br><br><br><br>

## [1.2.0] - 2026-08-14

### 创建示例Excel文件，仅做排布舞曲测试，该文件无法使用功能模块！

### 更换GUI界面皮肤，主要修改以下内容：

1，**导入替换**：

 - 将 import tkinter as tk 替换为 import ttkbootstrap as ttk

 - 添加 from ttkbootstrap.constants import *

 - 添加 from ttkbootstrap import Style

2，**主题设置**：

- 在主窗口创建时使用 ttk.Window(themename="pulse-dark")

- 在 __init__ 中使用 Style(theme="pulse-dark") 并配置全局样式

3，**bootstyle参数应用**：

 - "加载"按钮：bootstyle="primary"（蓝色）

 - "浏览"和配置按钮：bootstyle="secondary"（灰色）

 - "开始搜索"、"保存排布结果"和功能按钮：bootstyle="success"（绿色）

 - "应用固定位置"：bootstyle="warning"（黄色）

 - "停止搜索"、"清空"：bootstyle="danger"（红色）

4，**布局重构**：

 - 使用 grid() 替代 pack() 进行主要布局

 - Labelframe 使用不同的 bootstyle 颜色区分功能区域

 - 添加了适当的 padding 和间距

5，**现代化控件**：

 - Checkbutton 使用 bootstyle="round-toggle" 开关样式

 - Entry 自动获得圆角和内阴影

 - 状态标签使用 relief="sunken" 凹陷效果

6，**字体统一**：

 - 全局设置使用 "Microsoft YaHei" 字体

 - 不同控件使用不同的字体大小

7，**对话框更新**：

 - 所有弹窗对话框也使用 ttkbootstrap 组件

 - 保持一致的 bootstyle 样式
<br><br><br><br><br>

## [1.1.0] - 2026-08-13

**排布结果自动存储关键改动说明**：

✅ 自动获取脚本目录：使用 os.path.dirname(os.path.abspath(__file__))

✅ 固定文件名：舞曲排布_示例.xlsx

✅ 固定工作表名：排布结果

✅ 自动检测空列：从A列开始，找到第一个空列写入

✅ 每次都是新列：多次运行会依次填充到B、C、D...列

✅ 错误处理：包含文件占用、权限等异常处理

✅ 成功提示：显示保存路径、工作表和列信息

这样每次运行都会自动在当前脚本目录下创建或更新 舞曲排布_示例.xlsx 文件，新的排布结果会自动填充到下一个空列中。
<br><br>

**Excel文件的"条件记录"工作表中**：

A列单元格：生成时间-记录生成日期时间（格式：月日时分）

B列单元格：尝试次数-记录生成条件（搜索次数）

C列单元格：结果列名-记录该列的名称（如"A列"、"B列"等）

D列单元格：固定位置-记录生成条件（固定位置）

E列单元格：舞种间隔-记录生成条件（间隔值）

F列单元格：应用规则-记录生成条件（规则）

G列单元格：功能配置-记录生成条件（功能配置）
<br><br>

这样你就可以清楚地知道每一列数据是在什么条件下生成的，方便追溯和对比不同方案的排布效果。
<br><br><br><br><br>



## [1.0.0] - 2026-08-08

### ✨ 首版介绍
- **核心排布算法**：实现基于随机搜索的舞曲智能排布引擎
  - 支持"同一舞种至少隔开6首"规则
  - 支持"连续3首大类不能相同"规则
  - 支持"中速/慢速不三连，不允许快-快连续"规则
- **图形界面 (GUI)**：基于 Tkinter 构建完整的桌面应用
  - 舞种列表管理，支持数量调整
  - 独立规则开关，灵活启用/禁用约束
  - 实时搜索进度显示，支持随时停止
  - 排布结果按大类分组展示
- **数据管理**：
  - 支持从 Excel 文件加载舞曲数据
  - 内置 30+ 舞种的示例数据（摩登舞、拉丁舞、交谊舞等）
- **多线程优化**：搜索过程不阻塞界面响应

### 🐛 修复
- 无（首次正式发布）

### 📝 文档
- 完整的 README 项目说明
- 使用说明和常见问题解答
- 添加 MIT 许可证

### 🚀 发布说明
- 提供 Windows 可执行文件 (.exe) 绿色压缩包
- 包含 Excel 数据模板和使用说明

---

## [Unreleased]

### 计划中的功能
- [ ] 完善固定位置功能（指定某首舞曲必须在特定位置）
- [ ] 添加导出结果功能（TXT / Excel 格式）
- [ ] 支持自定义排布规则
- [ ] 优化算法性能，支持更大规模数据
- [ ] 添加进度条显示搜索进度
- [ ] 支持拖拽调整舞曲顺序
- [ ] macOS 版本打包 (.app)